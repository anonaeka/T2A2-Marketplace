<div class="container-fluid">
<div class="row">
  <div class="col-md-6">
    <div class="card-body">
          <% if @product.image.attached? %>
          <%= image_tag @product.image, crop: :scale, width: "100%" %>
          <% else %>
          <img src="https://res.cloudinary.com/ddqegwuzs/image/upload/v1621830575/Noimage_zgaeta.png" alt="" width="100%">
          <% end %>
    </div>
  </div>
  <div class="col-md-6">
    <div class="card-body">
    <h3><%= @product.name %></h3>
    <p><%= number_to_currency (@product.price) %></p>
    <p><%= @product.description %></p>
    <br>
    <div class="left">
      <%= link_to "Buy", new_product_order_path(@product), class: "btn btn-primary" %>
    </div>
    </div>
  </div>
</div>
<br>
<% if current_user == @product.user %>
  <%= link_to 'Edit', edit_product_path(@product), class: "btn btn-secondary" %>
<% end %>
  <%= link_to 'Back', products_path, class: "btn btn-warning" %>